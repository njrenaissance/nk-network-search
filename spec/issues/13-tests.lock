# Test lock for issue #13 (analysis.py — fitness/diversity curves and the
# inverted-U figure).
#
# Pins the git blob SHA (`git hash-object <path>`) of each committed test file
# at the point this plan was approved. Build's pre-push test-integrity guard
# verifies the file still hashes to this value — Build may add new tests, but
# may not weaken, delete, or reinterpret these ones. A genuine change to this
# test file requires a new Planner-approved lock, not a Build edit.
tests/test_analysis.py  ea08fc489da29c3d8ae1fabc99ab44037aea5ce2
