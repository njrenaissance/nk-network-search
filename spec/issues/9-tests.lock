# Test lock for issue #9 (Agent).
#
# Pins the git blob SHA (`git hash-object <path>`) of each committed test file
# at the point the human approved this plan. Build's pre-push test-integrity
# guard verifies the file still hashes to this value — Build may add new
# tests, but may not weaken, delete, or reinterpret these ones. A genuine
# change to this test file requires a new Planner-approved lock, not a Build
# edit.
tests/test_agent.py  1c75759498f1c70455727f65797ef1d0a91f7553
